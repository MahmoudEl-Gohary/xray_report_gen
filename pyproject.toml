[project]
name = "xray-report-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0"
]

training = [
    "torch>=2.0.0",
    "Pillow>=10.0"
]

evaluation = [

]